# {项目名} 代码库导读

> 生成时间：{日期}
> 分析范围：{根目录路径}

## 项目概览

- **语言**：{主要语言}
- **框架**：{框架，如无则写"无"}
- **规模**：{文件数} 个文件，{目录数} 个目录
- **类型**：{CLI 工具 / Web 应用 / 库 / ...}

## 目录结构

```
{目录树，标注每项职责}
```

| 目录 | 职责 |
|------|------|
| `agent/` | Agent 核心逻辑（主循环、上下文管理、提示词） |
| `tools/` | 工具集实现 |
| ... | ... |

## 入口点

| 入口 | 路径 | 说明 |
|------|------|------|
| CLI 入口 | `agent/cli.py:main()` | 命令行参数解析与启动 |
| ... | ... | ... |

## 核心模块

| 模块 | 路径 | 职责 | 被依赖次数 |
|------|------|------|-----------|
| ... | ... | ... | ... |

## 架构图

```mermaid
graph TD
    ...
```

## 关键依赖

### 内部依赖（基础设施）
- `tools/base.py` — 被所有工具模块依赖

### 外部依赖
| 库 | 版本 | 用途 |
|----|------|------|
| ... | ... | ... |

## 常用命令

```bash
# 安装依赖
pip install -r requirements.txt

# 运行测试
python -m pytest tests/

# 启动
python -m agent.cli "任务描述"
```
